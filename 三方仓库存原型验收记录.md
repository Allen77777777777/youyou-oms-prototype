# 三方仓库存原型验收记录

日期：2026-09-23。状态：本地交互原型已完成，业务方案待评审，未开展真实接口验收。

## 1. 入口与实现

- 入口：OMS → 仓配管理 → 三方仓库存；沿用现有全局壳层、菜单抽屉与页面页签。
- 路由：`/oms/warehouse-delivery/inventory`。
- 本地预览：http://localhost:5173/#/oms/warehouse-delivery/inventory 。本轮未部署公开站点。
- 已实现组合查询、批量 SKU、数量筛选和排序、分页、只读详情、九项业务字段说明、刷新、XLSX 导出与原型标注。
- 中文字段不加“三方仓”前缀；来源通过页面说明表达。库存不增减、不汇总、不按订单推算、不转换单位。
- 62 条虚构记录覆盖五家服务商、多账号、同仓库 SKU 跨账号、零数量、未取得、未提供、负数、箱单位、映射缺失／冲突、更新失败和本次未确认。

## 2. 已验证

| 检查 | 结果 |
| --- | --- |
| `pnpm typecheck` | 通过 |
| `pnpm test` | 12 个测试文件、81 项测试通过，其中库存新增 7 项 |
| `pnpm build` | 通过；已有公共包超过 500 kB 的构建提示仍存在 |
| 导航 | OMS 菜单包含新入口；物流渠道管理与库存页签可切换；全局搜索可找到并打开库存 |
| 查询 | 系统 SKU 精确查询返回多条原粒度记录；可用数量等于 0 返回 9 条，未取得不纳入零值；重置恢复 62 条 |
| 详情及定义 | 抽屉可开关；数量和来源时间可读；字段说明包含库存单位及八项数量业务定义 |
| 标注 | 开关正常，索引可见，标注开启时仍可重置查询 |
| 桌面布局 | 检查 1280×900、1440×900、1920×1000；页面容器不横向溢出，较窄视口通过表格内部滚动查看右侧列 |
| XLSX | 浏览器确认导出显示“已生成 62 条库存记录的文件”；生成器输出通过 ZIP CRC、所有 XML 解析与 openpyxl 读取，包含 62 条记录／29 列；验证 SKU、数值、空值、缺失原因、箱单位 |

导出自动化未捕获到内置浏览器的下载事件，未确认浏览器最终保存位置；上述文件内容检查针对同一生成器的实际输出，不将其表述为已验证所有浏览器下载落盘。

## 3. 演示边界与待确认

真实库存、中台或海外仓接口、服务端权限与审计、后台大批量导出、网络失败重试均未实现。样例服务商名称不表示该仓接口已经接通。固定时点及“可能过期”仅用于评审数据提示，不构成生产时效阈值。

本轮查询提示暂为单选、更新时间按 UTC+8 自然日筛选、默认采用固定样例顺序。SKU 大小写按原字符串精确匹配，正式规则需复用中台。详情宽度为 `min(900px, 视口宽度－80px)`；数量排序作用于完整匹配集、空值置后。

4PX 批次／质量／条码和来源粒度冲突、商品图片、SKU 复制及订单页面库存联动未实现。库存页可接受 `sku`、`warehouseId`、`accountId` 查询参数，未扩展其他订单页面。PRD 的正式功能与验收要求不因本轮演示边界而删除。

本轮保留原有订单处理、订单标发等未提交修改；没有改动其业务实现。原型可交互不代表真实业务、权限、性能或接口已通过验收。

## 4. 同日页面批注修订

删除顶部标题整行和底部提示文案；筛选下拉框固定 200px、关键词框 300px，按可用空间换行。移除列表“数据提示”列，详情保留异常原因。库存单位及八项数量全部列出，表头悬停可读业务定义，字段说明入口移至详情底部。删除样例中的共享库存字段及列表、详情、导出对应展示，XLSX 由 29 列调整为 28 列。同步功能 PRD V0.6 与字段定义 V0.5。

本轮复核：typecheck、81 项测试及 build 通过；浏览器检查 1280／1440／1920 宽度均无页面横向溢出，三个主筛选下拉框实测均为 200px，表头包含完整八项数量且无“数据提示”列。精确查询返回 11 条，重置恢复 62 条，详情没有库存范围，标注模式下重置正常。表格横向滚动条常显，供窄屏查看全部库存列。
