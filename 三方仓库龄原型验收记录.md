# 三方仓库龄原型验收记录

验收日期：2026-09-23。依据：《三方仓库龄功能 PRD》V0.5 及《三方仓库龄字段定义》，含当日页面批注修订。本记录累计记录前端原型验证，不代表真实接口或生产业务验收。

## 交付内容

- 入口：OMS → 仓配管理 → 三方仓库龄。
- 本地预览：[三方仓库龄](http://localhost:5173/#/oms/warehouse-delivery/inventory-aging)。本轮未发布到公开站点。
- 在现有 Vue 原型工程实现分段汇总、批次明细、查询、分页、刷新、XLSX 导出及原型标注，沿用全局壳层、菜单抽屉和页签。
- 使用 80 条虚构快照明细；默认最近日期 2026-09-23 有 75 条明细，按身份组成 62 条仓库 SKU 主行或 62 组分段汇总。两种视图共用来源明细，库龄天数直接使用来源值。
- 按页面批注删除全局采集失败提示条、顶部虚构演示数据文字及底部来源说明。批次明细改为 SKU 主行展开子明细；中文字段改为“入库委托单号”，保存原始 `consignment_no`。
- 本轮将批次子表“在库数量”拆为“在库数量（良品）”与“在库数量（不良品）”；删除两种视图的“数据说明”列，XLSX 同步拆列并移除“数据提示”。原始来源数量与技术校验仍保留。
- 同步 PRD、字段定义、工程 README 与项目入口说明。原有未提交修改保留。

## 已验证

| 项目 | 验证结果 |
| --- | --- |
| 静态检查与自动测试 | 本轮 `pnpm typecheck`、`pnpm test`、`pnpm build` 通过；共 15 个测试文件、114 项测试。库龄模型、导出、页面交互共 33 项；新增质量数量映射与导出断言，保留 3 项真实 Element Plus 集成测试。 |
| 导航与全局功能 | OMS 菜单抽屉可打开库龄页面；库存与库龄页签可切换；全局搜索能找到并打开“三方仓库龄”。 |
| 双视图与查询 | 仓库 SKU 精确查询、批次及库龄区间查询生效。切换视图保留共用条件、清空明细专属条件与勾选，并返回第一页。 |
| SKU 与批次展开 | `DEMO-FLT-002` 查询结果为 1 个 SKU 组、11 条批次明细；默认折叠，展开子表按来源库龄降序展示。按批次 `000003` 查询后只剩 1 条匹配子明细，并收起展开、清空勾选。组件测试同时覆盖同批次不同入库委托的来源行全部保留。 |
| 汇总维度与分段 | 同一组两条来源数量 3 和 5、库龄均为 45 天时显示 8；不同仓库、账号、客户、质量及单位不混合。模型测试覆盖分段边界、缺失数量、非法库龄、重复记录及未知单位。 |
| 质量数量与列删减 | 本轮浏览器实测良品批次显示“16 / —”，不良品批次显示“— / 2”；两视图表头均无“数据说明”。模型与 XLSX 测试覆盖真实零、空值、负数、小数及未知质量，不从未知或原始 G/E/R/B 值自行推断良品/不良品。 |
| 历史日期与刷新 | 选择 2026-09-22 展示该日记录；刷新不改变快照日期及来源更新时间。选择无数据日期 2026-09-20 显示空结果，不借用其他日期数据。 |
| 采集失败场景 | 原有日期模型验证继续通过：失败日期无记录，不用历史值补齐当天。本轮全局失败提示条已移除；组件及浏览器检查确认页面无该提示区。 |
| 分页及勾选 | 默认每页 50 条；批次视图按 SKU 主行分页、勾选。组件测试使用 52 个 SKU 组，验证第二页勾选并展开后查询会回第一页、清空勾选及展开，不截断子明细。 |
| 导出 | 前轮浏览器勾选折叠的 1 个 SKU 组，确认页正确显示 11 条明细并生成成功；本轮组件测试继续核验工作簿输入包含全部匹配来源行。XLSX 单元测试验证新版两项数量表头、移除“数据提示”、文本标识、数量精度、空值及导出数据固定。未单独核验浏览器最终下载目录中的文件。 |
| 标注 | 开启标注后可以定位字段与规则说明；SKU 查询等业务输入仍可正常操作，关闭标注后恢复常规页面。 |
| 桌面布局 | 本轮在 1280、1440 实际检查含两项数量的展开批次子表，在 1920 检查删列后的汇总视图；1440 下核验新增数量字段的标注定位。三种宽度均无页面级横向溢出或关键文案重叠，表格采用内部横向滚动；扩展列布局在前轮已验证。 |

构建保留现有共享资源包超过 500 KB 的提示，不影响本轮构建成功。

## 未验证与待评审

- 真实海外仓 API、每日采集及快照保存任务、服务端权限、导出审计均未接入；本地刷新仅重新读取演示数据。
- 分段阈值、汇总规则、缺失值处理仍按 PRD 的评审提案展示。单位未提供时整组暂停汇总是谨慎的原型提案，尚非已确认生产规则。
- 真实接口的来源维度、重复记录判定、分页完整性及失败处理仍需联调；不得将本原型的虚构字段值作为仓库接口事实。
- “入库委托单号”的命名有 4PX 官方入库接口同名字段依据，库龄与入库接口实际编号对应仍待核验，不等同于优优采购入库单号或客户参考号 `ref_no`。
- 用户已确认 SKU 主行展开多批次及三处提示删减；默认折叠、主行分页勾选、平铺明细导出属于本轮实现方案，具体口径见 PRD。
- 用户已确认良品与不良品数量拆列、删除汇总“数据说明”；按明确质量映射至对应列、另一列留空及同步删除子表/导出说明为本轮实现方案。真实仓库质量枚举及映射仍待联调，未知分类的原始数量保留在技术追溯资料中。
- 未进行生产数据、并发批量导出、真实权限范围及性能容量验收。本轮不涉及库存增减、订单联动或独立库存账。
