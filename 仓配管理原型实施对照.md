# 仓配管理原型实施对照与验收说明

| 项目 | 内容 |
| --- | --- |
| 文档编号 | PRD-OMS-WAREHOUSE-DELIVERY-001 |
| 最后更新 | 2026-09-12 |
| 状态 | 原型实施记录与评审说明；不作为第二份业务权威 PRD |
| 页面 | 三方仓出库单、物流下单、物流渠道管理 |
| 入口 | OMS → 仓配管理 |

## 1. 定位与事实等级

对应业务方案以[三方仓出库单功能 PRD](../PRD/三方仓出库单功能PRD.md)、[物流下单功能 PRD](../PRD/物流下单功能PRD.md)、[物流渠道管理功能 PRD](../PRD/物流渠道管理功能PRD.md)及[六模块方案总览](../PRD/OMS履约六模块PRD方案总览.md)为评审入口。本文只记录本次原型实现、未实现的条件能力、演示边界及验收路径，不建立另一份权威定义。原型标注直接引用各独立 PRD；本文件仅用于原型实现对照。

2026-09-12 对照上述并行形成的 PRD 草案：七态/五态与核查门禁一致；真实仓库撤单、物流作废/替代、面单下载、外部渠道同步、接口验证、组合发布及在途影响计算尚未实现。物流原型提供显式本地样例，用于本次获授权的流程评审，不能据此宣称该生产组合已启用。渠道“业务启用”与实际账号/组合可用性分开显示，所有样例仍待核对，不因为本地启用就通过外部能力门禁。

三方仓出库单回答仓库是否受理、是否出库以及异常如何核查；物流下单回答是否已经取得成功有效的面单结果；物流渠道管理承载候选标准渠道和外部渠道映射配置。三个页面分别展示所属业务对象，不通过页面直接改写 OMS 订单六态。

2026-09-12 用户明确要求开始制作这三个功能的高保真原型。该决定授权原型实现，不将此前无一期规划的 API 物流场景自动升格为一期生产交付。

依据为[订单单据字段标准](../PRD/订单单据字段标准.md)第 9、10、13 节、[订单处理功能 PRD](../PRD/订单处理功能PRD.md)、[领域模型与状态机](../文档/架构/领域模型与状态机.md)、[OMS 需求概述](../文档/产品规划/OMS需求概述.md)。流程上游为[钉钉《跨境订单履约业务流程》](https://alidocs.dingtalk.com/i/nodes/Amq4vjg895n2OA9yfx5D1A2r83kdP0wQ)。本次在线正文读取未成功，采用本地文档记录的 2026-09-10 核验基线与用户后续明确决定，未宣称本次在线核验成功。

| 等级 | 本稿内容 |
| --- | --- |
| 已确认 | 四套状态机独立；出库七态、物流五态；外部待核查禁盲重试；复用仓库授权和现有接口；不新增库存同步、平台面单和 OMS 取消申请 |
| 合理假设 | 列表列顺序、卡片、查询条件、详情抽屉、当前页批量、渠道配置表单、启停及唯一校验方案；用于原型评审，不代表已获批生产规则 |
| 待确认 | 五家仓库字段/能力映射、查询频率、撤单责任、渠道主数据读写边界、API 物流适用组合、权限与业务量级 |

## 2. 共性页面与执行约束

- 角色假设：履约操作员跟进执行；仓库/物流接入支持核查请求和映射；配置管理员维护候选渠道。实际岗位及权限矩阵待确认。
- 列表查询：业务单号、系统订单号、履约单号、外部单号、跟踪号或系统 SKU；服务商、处理关注和创建日期取交集。点击搜索或 Enter 应用，重置恢复全量。日期明确采用 UTC+8 展示，生产存储应带时区或采用 UTC。
- 页签计数：按当前筛选匹配的出库单/物流单数量统计；一张履约单的多条历史执行记录分别保留，不将订单数与执行单数混加。
- 卡片统计全部当前会话单据；“执行中”覆盖下单中、取消中、待出库（按各模块存在的状态），点击进入相同集合。“待核查”是标记，不新增业务状态。
- 主编号和详情入口打开侧边详情；详情包含业务快照、状态时间线和接口尝试。外部编号未取得时展示空缺，不补造。生产原始报文需受控访问，原型不保存凭据、收件明文或真实接口响应。
- 勾选仅限当前页。切换状态、翻页、执行查询或重置清空选择。批量操作先展示总数、可执行与被拦截范围；确认时重新逐项检查。结果保留每项失败原因与下一步。
- 批次成功/失败是本次操作结果；“业务处理中”和“待核查”是业务状态维度，可重叠，不能与成功/失败相加。
- 生产执行须按对象 ID、账号范围、动作、版本和幂等键受控，不能用展示文案驱动状态机。重试保留原业务编号与历史；未确认外部结果前禁止重复请求。
- 本地演示状态放在会话共享模块，切换中台页签保留，浏览器刷新重新初始化。所有数据、查询回放和写动作在页面明确标为本地演示。

## 3. 三方仓出库单

### 3.1 状态与数据

唯一业务状态集合：下单中、下单失败、待出库、出库异常、已出库、取消中、已取消。原始仓库状态、接口尝试结果和异步任务结果分别保存。

| 页面字段 | 权威来源与口径 |
| --- | --- |
| OMS 出库单号 | `warehouse_outbound_order.outbound_order_no`，系统内部展示编号 |
| 来源履约/订单 | `fulfillment_order_id` 及履约明细来源；一个履约单可存在多次历史执行 |
| 仓储服务商/仓库/账号 | 出库单实际执行快照，授权账号引用不包含密钥 |
| 物流渠道/渠道代码 | 出库单/履约单的本次执行快照；主数据改名不回写历史 |
| 外部仓库单号 | `external_outbound_no`，唯一性需结合仓库账号 |
| 计划/已出库数量 | 出库明细 `planned_quantity` / `shipped_quantity`；明确仓库事实才增加已出库量 |
| 跟踪号 | `tracking_number`，不得作为全局唯一标识，也不能替代实际出库事实 |
| 失败原因/核查标记 | 对应业务对象、接口尝试和日志的可读投影，不创建异常单 |

### 3.2 操作与门禁

1. 已明确下单失败且前置条件已满足的单据可演示重新提交：在原业务单上转为下单中，等待明确受理，不直接成功或出库。
2. 接口超时的建单单据保持下单中，取消请求未确认保持取消中；“出库异常”仅承载建单成功后的仓内业务异常。
3. 核查按钮先展示待查询对象和预期回放说明。独立仓配样例回放固定“原请求明确未受理”，由下单中变为下单失败后才开放重提；取消样例仍未取得结论时继续保持取消中。用户不可手填成功状态或外部单号。
4. 从订单处理推入的出库单消费[演示会话](../原型/src/页面/订单处理/演示会话.ts)的共享执行记录，并调用同源受控核查函数。正常新推单明确受理后进入待出库，订单处理按全部有效履约范围重新聚合；超时专用样例继续待核查。不得由另一个页面的静态样例覆盖共享事实。
5. 下单失败但渠道权限或映射尚未修复时保持拦截。渠道原型保存不自动证明外部权限已恢复，不静默解除执行阻断。
6. 已出库单据禁止重新提交建单，查询只补充可信事实，不回退出库数量。部分出库保留数量差异，不自动升格为整单已出库。
7. 本页展示取消中/已取消的已有执行事实，不提供订单取消申请、人工改为已取消或仓库撤单发起入口。撤单发起方与库存释放责任尚待评审。

## 4. 物流下单

### 4.1 适用性与状态

仅演示自有物流 API 先取得面单/跟踪号再推仓的目标流程；一期适用场景尚待评审。页面不提供平台面单获取。

业务状态为待下单、下单中、下单成功、下单失败、已作废。结果未明确时进入下单失败，并通过失败原因标记待核查；不得新建“不确定”等状态。

主字段来自 `logistics_order`：内部物流单号、履约单、服务商、账号引用、渠道及外部渠道代码快照、外部物流单号、跟踪号、面单引用、成功时间、幂等键与申报快照批次。原型面单仅显示虚构引用，不提供真实文件或虚构生产下载。

### 4.2 执行路径

| 动作/触发 | 前置条件 | 结果与异常 |
| --- | --- | --- |
| 提交下单 | 当前待下单且没有核查标记和前置阻断 | 本地受理后进入下单中；重复点击/重放被状态门禁拒绝 |
| 查询待核查失败 | 有原请求且业务未作废 | 回放固定明确未受理样例，仍为下单失败并清除待核查标记；保持原单和查询证据 |
| 恢复待下单 | 下单失败、外部结果已明确且前置条件满足 | 原单回到待下单；后续需再次提交，不直接写成功 |
| 查询已有结果 | 有可查询请求 | 保留当前可信状态，追加尝试说明；不造新运单或出库事实 |
| 已作废 | 历史事实 | 不允许重提；作废及外部取消能力未在原型开放，待接口和责任评审 |

依赖面单的推仓动作必须等待成功且有效的物流结果。物流单成功不等于仓库受理或已出库；本页不直接覆盖其他对象状态。生产版须补齐真实跟踪号/面单有效性、文件权限、币种精度、收费及退费边界，不能从原型样例推导。

## 5. 物流渠道管理

### 5.1 候选配置结构

以下配置模型属于合理假设，应与中台主数据能力评审后确定写入方。原型仅开放三方仓物流、仓库回传面单。

| 字段 | 原型校验/语义 |
| --- | --- |
| 渠道名称 | 必填，去除首尾空格，最多 60 字 |
| 标准渠道代码 | 必填，2–40 位大写字母/数字/下划线/短横线，本地唯一；编辑已有渠道时固定 |
| 仓储服务商 | 从五家已确认目标服务商中选择，选择不代表该服务商已完成验收 |
| 仓库账号引用 | 从既有授权引用选择；原型仅虚构账号，不收集 token |
| 发货仓库 | 选择演示仓库，真实账号/仓库关联校验待主数据契约确认 |
| 服务商渠道代码 | 必填，最多 128 字；服务商+账号+仓库+外部代码组合不得重复 |
| 面单获取模式 | 固定仓库回传，不开放平台面单 |
| 目的国家/地区 | 至少选择一项；覆盖地区为虚构配置，真实范围待服务商确认 |
| 启停与更新时间 | 新增默认停用；启停单独确认；时间按 UTC+8 展示 |

### 5.2 交互和变更

- 新增/编辑在侧边表单中进行。取消关闭不保存；校验失败保留全部输入并展示具体原因；成功后刷新列表和本次会话日志。
- 保存新渠道只形成停用草案。启用前再次校验必填与唯一性，二次确认后更改。
- 停用确认展示名称、代码和影响说明：不再作为后续路由选择候选，历史执行快照保留。不自动取消或重规划在途单据。
- 启停与保存记录操作者、时间、对象和操作说明。生产版还应记录前后值、原因和版本；审批流程及角色矩阵待评审。
- 原型频道配置未联动实际仓库账号，不证明服务商权限可用；也不自动解决已存在出库失败。前置能力确认应独立取得证据。

## 6. 原型验收与上线边界

### 6.1 可执行原型用例

| 编号 | 验收结果 |
| --- | --- |
| AC-WD-01 | 三方仓仅七态、物流仅五态；待核查不成为额外状态，角标和卡片计数单位一致 |
| AC-WD-02 | 搜索、日期、服务商与状态取交集；无结果可重置；翻页清空当前页选择 |
| AC-WD-03 | 物流超时失败不能重提；查询明确未受理后才可恢复待下单，再提交进入下单中 |
| AC-WD-04 | 仓库建单超时保持下单中；撤单未明确保持取消中，均不能盲目重试 |
| AC-WD-05 | 混合批量明确失败、待核查、已出库/已成功时逐项反馈，已完成事实不回退 |
| AC-WD-06 | 每次有效演示追加时间线/接口尝试；重试保留业务编号，不伪造成功、出库和面单文件 |
| AC-WD-07 | 订单处理推单后，三方仓列表可查同一履约单与出库单；核查后两个页面读取同源状态 |
| AC-WD-08 | 渠道空必填、重复代码、重复映射和无目的地时保存失败；合法新增为停用，启停需确认 |
| AC-WD-09 | 编辑/启停不删除渠道、不改历史执行快照；切中台页签回来仍保留当前会话变更 |
| AC-WD-10 | 1280、1440、1920 桌面宽度下无页面级横向溢出；开启标注后搜索换行、表格局部滚动，编号可定位且关闭后不拦截业务操作 |

自动化规则测试位于[仓配模型测试](../原型/src/页面/仓配管理/仓配模型.spec.ts)。页面与标注使用稳定 ID `oms.outbound.*`、`oms.logistics.*`、`oms.channels.*`，按页面、字段、交互、规则和待确认分类。

### 6.2 易仓替代映射与风险

| 项目 | 当前结论/门禁 |
| --- | --- |
| 易仓现有能力 | 三方仓建单、查单、异常、渠道映射、物流获取面单的实际岗位 SOP 与定制规则待调研 |
| 使用角色与量级 | 履约/接入/配置角色为假设；真实单量、峰值、批量上限、SLA 与责任人待确认 |
| 目标归属 | 出库与物流业务对象归 OMS，各自状态独立；授权与已建接口复用中台；库存同步留在中台 |
| 差异处理 | 保留外部事实和执行历史，禁止通过失败按钮盲目重复建单；平台面单继续排除 |
| 数据迁移 | 迁移范围、外部账号组合唯一键、幂等键、历史路径、失败与在途核查记录需形成映射并逐项对账 |
| 切换 | 按平台/店铺/仓库账号/渠道组合登记；先影子只读对账，再单写灰度，禁止 OMS 与易仓重复外部写 |
| 回退 | 停止新提交、核查在途、冻结责任台账、按批准的单写边界切回并补拉事件；保留全部外部单与出库事实 |
| 验收证据 | 五仓接口 PoC、账号/渠道映射、数量对账、重复通知与超时核查、取消竞争、回退演练和业务签字 |

本次是可交互原型交付，不表示完成真实接口、权限、迁移或易仓退出。生产版本上线前必须补齐状态映射、并发版本、鉴权失效、限流、乱序、重试上限、日志与监控，明确查询频率、告警阈值及升级责任人。

## 7. 待确认项

| 编号 | 问题 | 影响 |
| --- | --- | --- |
| Q-WD-01 | 五家仓库创建、查询、撤单、部分出库、自行拆包与原始状态映射 | 出库状态转换与数量事实 |
| Q-WD-02 | 撤单发起方、取消结果与库存释放时点 | 逆向履约责任，当前入口保持未开放 |
| Q-WD-03 | API 物流适用组合与一期/后续排期、费用和面单校验 | 物流页面生产范围 |
| Q-WD-04 | 标准渠道主数据读写方、账号-仓库-渠道真实能力和唯一约束 | 渠道维护、校验与规则选择 |
| Q-WD-05 | 渠道停用对已审核未推单、已推单与核查中对象的影响 | 在途重规划及执行门禁 |
| Q-WD-06 | 权限、批量上限、核查证据、人工恢复和审批要求 | 正式动作开放与审计 |
| Q-WD-07 | 查询频率、最大等待时长、告警、升级责任和业务量 | 性能与可观测性 |
| Q-WD-08 | 易仓现有 SOP、历史范围、组合级切换与回退 Runbook | 生产替代验收 |

上述待确认项尚无可写入的具体负责人及截止日期；二者也须在进入生产开发前确认，不由原型推测补齐。
