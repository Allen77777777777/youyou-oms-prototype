# 全渠道订单列表评审验收记录

日期：2026-09-14

## 本轮调整

- 清除无规则命中或来源依据的演示订单标签，不把“临、信、占”等样例当作订单事实。
- 列名改为“仓储物流”。
- 删除前端自行定义的“未接入”同步状态及“接口待接入”展示；没有同步事实时展示空值，不用集成进度替代业务状态。
- 商品摘要和商品行快览增加 52px 缩略图，读取商品图片引用；当前样例沿用 eBay 原型占位资产，并通过替代文本和提示明确占位性质。
- 移除查询方案、保存方案、另存为、自定义列整行及对应弹窗，不再恢复历史方案对列表的影响。
- 移除顶部多平台通用字段说明。同步更新 PRD 和原型标注。

## 验证结果

- `pnpm typecheck` 通过。
- `pnpm test` 通过：11 个测试文件、68 项测试。
- `pnpm build` 通过；仍有构建产物大于 500 kB 的体积提示。
- 浏览器验证关键词查询返回指定订单、重置恢复 25 条、分页前后切换、OMS 功能菜单、列表导航、原型标注打开和关闭。
- 列表图片加载成功；商品行快览包含两行图片和对应商品信息。
- 在 1280、1440、1920 桌面宽度检查布局，文档宽度均未超出视口；窄屏表格通过内部横向滚动查看后续列，筛选栏换行。测试后恢复默认视口。

## 数据边界

本轮验证使用虚构原型数据，没有验证真实平台接口或商品图片接口。已有“状态映射待接入”是平台状态缺失的展示占位，仍不写入平台状态字段；它与本轮删除的同步状态“接口待接入”不是同一个字段。
