# 订单标发与规则原型实施对照

本文件仅记录 2026-09-12 可运行原型的实现与验收范围，不另立业务权威。产品方案引用同日并行形成的[订单标发功能 PRD](../PRD/订单标发功能PRD.md)和[订单规则功能 PRD](../PRD/订单规则功能PRD.md)。下文业务描述用于说明模拟器边界，不替代上述 PRD、字段标准或平台专项规则。

当前实现与 PRD 提案的差异：标发页先实现单据筛选、提交、结果核查与尝试追溯；更多筛选、时效、自动调度与历史认领尚待完成。规则页先提供单层 AND/OR 条件、停用新建、启停、版本计数、顺序与只读试算；尚未实现有限嵌套条件、不可变发布版本库、生效时间及冲突检测。审单和标发页面中的“首条命中候选”仅用于原型说明，PRD 提出的系统安全检查全量执行、用户审单规则全量收集与结论优先级仍须评审，不等于当前演示器已实现正式执行引擎。

| 项目 | 内容 |
| --- | --- |
| 文档状态 | 首版交互草案；待业务、产品、研发与测试评审 |
| 最后更新 | 2026-09-14（同步业务口径，未扩大原型实现范围） |
| 订单标发入口 | OMS → 自发货订单 → 订单标发，`/oms/self-fulfillment/shipping-confirmation` |
| 订单规则入口 | OMS → 自发货订单 → 订单规则，`/oms/self-fulfillment/order-rules` |
| 原型性质 | 本地虚构数据与模拟响应；刷新重置，切换页签保留本次会话变更 |

## 1. 依据与边界

用户于 2026-09-12 明确要求开始制作订单标发、订单规则。本稿将已确认的领域边界与可评审的操作方案分开。

- **已确认**：数据模型与字段以[订单单据字段标准第 11 节](../PRD/订单单据字段标准.md#11-订单标发)为准；标发五态与其他业务对象独立。状态机参照[领域模型与状态机](../文档/架构/领域模型与状态机.md#84-订单标发状态机)的已核验本地副本。钉钉上游本次读取返回内容未就绪，不声称本次已在线复核。
- **已确认**：eBay 标发条件、按包裹提交、行数量约束、结果核查与幂等以[eBay 订单 API 对接说明](../文档/集成/eBay/eBay订单API对接说明.md#911-平台标发单到-ebay-fulfillment-的映射)为准；订单状态流转采用用户于 2026-09-14 定稿的第 11.5.1、11.5.2 节。
- **合理假设**：页面布局、筛选、批量资格、规则条件编辑、数值优先级、首条命中与试算呈现，用于本次原型评审。
- **待确认**：正式角色、批量上限、权限矩阵、规则发布审批、生效时间、平台行级部分结果、人工标发认领和豁免场景。

订单处理与全渠道订单使用同一主事实。标发只更新 `platform_shipment_confirmation` 及明细、接口尝试和日志，不反写有效出库量，不把“已发货”回退为“待发货”。没有有效出库不得因规则命中提前标发。

不提供取消申请、退款操作、SKU 配对、库存同步、平台面单物流发货，不修改 eBay 专业订单页面。其他未接入平台不借演示页面伪造已验证标发接口；首版可执行标发演示仅使用独立虚构 eBay 来源订单。

## 2. 订单标发

### 2.1 使用角色与目标

订单运营/履约人员查找待回传或失败单据，核对出库来源，查询平台结果并在资格满足后提交；技术支持核对接口尝试。角色名称及可操作店铺范围为合理假设，实际权限由中台能力与业务负责人确认。

### 2.2 列表与详情字段

默认一行一张平台标发单，不将标发数量称作订单数。来源订单、履约单、实际出库明细以及平台来源标识必须可关联追溯。

| 信息区 | 内容 | 来源/写入方 |
| --- | --- | --- |
| 标发身份 | 标发单号、来源系统订单号、来源平台订单、履约单 | OMS 标发对象与关联订单 |
| 平台与店铺 | 平台、店铺名称及授权范围 | 来源标准订单，实际调用凭据受控注入 |
| 状态 | 待标发、标发中、标发成功、标发失败、无需标发 | 标发中心；不是订单处理状态 |
| 提交快照 | 承运商代码、跟踪号、实际发货时间、来源订单行、数量 | 有效履约与出库事实、平台适配器 |
| 幂等证据 | 稳定幂等键、每次提交/查询尝试 | 标发中心与集成层 |
| 结果 | 失败原因、是否平台结果待核查、成功回执、豁免原因 | 平台响应/查询证据或已确认豁免结论 |
| 尝试历史 | 时间、动作、结果、证据摘要 | 接口调用尝试；普通界面不暴露请求原文或令牌 |

列表提供五态页签、待回传/失败/待核查/成功数量、编号及跟踪号查询、平台筛选、选中数量和批量提交。筛选无结果时可清空条件。执行单据详情可以使用抽屉；来源标准订单的完整详情仍在应用内独立页签打开。

### 2.3 五态及失败处理

| 当前情形 | 允许动作与结果 |
| --- | --- |
| 待标发 | 校验通过并确认后进入标发中；拒绝或不明响应进入标发失败；明确成功才进入标发成功。 |
| 标发中 | 可查询平台结果；不允许重复提交。 |
| 标发成功 | 展示平台回执与尝试证据，不开放重复提交。 |
| 标发失败，明确未成功 | 修复已知原因并重新校验后可重试；复用原标发单和幂等键。 |
| 标发失败，平台结果待核查 | 只查询/对账，不允许人工或自动盲目重提。查询到等价 fulfillment 则原单补记成功；明确未创建后才能按规则重试。 |
| 无需标发 | 必须保留明确豁免原因。原型仅提供“演示豁免”样例，实际适用场景待确认；不提供随意将失败单改为无需标发的入口。 |

结果不明是失败原因分类，不增加独立第六态。eBay 请求超时、可能已送达的 5xx、解析失败、201 但缺少可解析 Location 等写入 `EXTERNAL_RESULT_PENDING_VERIFICATION`，`is_retryable=false`。核查等价记录必须比较来源订单行、数量、承运商与跟踪号，并保留外部证据和来源归属。

### 2.4 提交前置、批量及重复请求

1. 来源订单和履约单有效且关联一致；当前为待标发或可安全重试的标发失败。
2. 正式 eBay 实现须读取当前订单级 `orderPaymentStatus=PAID`，并按专项规则核查取消状态、既有 fulfillment 和本地履约事实；付款明细及本地付款时间不能替代订单级付款判断，也不追加退款明细或纠纷条件。当前标发原型不再以虚构 `paidAt` 作为门禁；尚未接入实时订单级付款校验，页面只消费准入及平台增量链路形成的 OMS 状态结果，这仍属于实现缺口，不代表生产校验已完成。
3. 存在明确有效出库数量及实际发货时间；跟踪号、承运商和提交时间完整。数量不得超过有效已发货数量。eBay 同一行全部应标发数量必须由一个实际包裹及跟踪号覆盖。
4. 同一业务版本未重复标发；实际应按平台、店铺、幂等键与明细净数量在服务端强约束。前端按钮禁用不能代替服务端并发校验。
5. 批量选中后逐单计算总数、可执行与跳过数。二次确认明确范围；确认后逐单再次校验，展示成功、失败、跳过、处理中以及跳过原因，最终清除勾选。
6. 标发失败不触发重复物流下单、重复推仓、重复仓库出库，也不回退订单已发货事实。

本地提交使用模拟明确成功响应；另预置标发中、明确拒绝可重试、超时待核查、成功、豁免六类单据。查询待核查样例返回预设等价记录并补记成功，不让用户任意输入业务成功结论。实际网络错误、并发、权限、数量分摊与部分结果属于后续接口验证。

## 3. 订单规则

### 3.1 类型与作用

依据[OMS 需求概述](../文档/产品规划/OMS需求概述.md)，首版呈现审单规则、分仓规则、订单标发规则三类。是否设独立物流规则仍待确认，不因页面制作自行纳入。规则配置必须服从独立业务门禁，不能通过配置关闭订单级 `PAID` 准入、准入后非 `PAID` 转异常、取消、平台接入、有效出库和幂等约束。

| 类型 | 原型候选动作 | 边界 |
| --- | --- | --- |
| 审单规则 | 进入人工复核；满足门禁后建议自动审核 | 当前只给出建议，不自动写审核结论；正式每轮审核仍将检查命中和结论汇总到 `decision_note`。 |
| 分仓规则 | 将五家仓库中的具体仓设为候选 | 不在试算中写订单路由、占用库存或创建履约单；待审核阶段路由写入依订单处理规则完成。 |
| 订单标发规则 | 具备出库事实后建议标发；进入人工核查 | 不自动创建或推进标发单，不将规则命中等同实际已发货。 |

### 3.2 条件编辑与版本

**合理假设**：使用平台、店铺、目的国家和履约模式四个通用条件字段；提供等于/不等于和“全部满足 AND / 任一满足 OR”。选项来自本地标准订单，可输入条件值；未命中必须明确反馈。正式可配置字段、校验及合法值由业务规则专项评审。

- 新建规则需要名称、至少一个完整条件和有效候选动作；默认停用，防止未评审配置立即影响试算。
- 同类规则名称在原型中唯一。缺名称、空条件、无动作时保留编辑草稿并提示修复。
- 编辑基于独立副本；取消不改变原规则。保存生成新版本并记录时间；启用、停用与优先级变更也产生版本和会话审计。
- 启停二次确认说明后续试算影响，已形成履约执行快照不变。
- 上移/下移交换同类相邻优先级，保存两条新版本及同一变更时间。不同规则类型不比较优先级。
- 规则和变更记录在模块级会话保存，切换页签仍可查看，浏览器刷新重置。正式持久化、团队权限、乐观锁、版本发布与生效时间尚未实现。

### 3.3 只读试算与冲突策略

**合理假设**：同类规则按优先级数值从小到大匹配，首条命中作为候选，全部命中轨迹保留。多个规则是否叠加、允许覆盖哪些字段、无命中默认动作、冲突分层和优先级重复策略均待确认。

1. 选择一个本地标准订单，点击开始试算；仅当前规则类型参与。
2. 记录规则版本快照，逐条显示停用跳过、未命中、首个命中、后续命中。历史试算结果不随之后的规则编辑静默变化。
3. 候选动作与门禁分别显示。平台履约、未进入处理、终止/异常、未接入平台阻断执行；审单与分仓需要待审核阶段；标发需要明确有效出库。订单级 `PAID` 准入及准入后非 `PAID` 转异常由平台适配链路负责，标准订单 `paidAt` 仅展示，不作为规则试算门禁。
4. 本地门禁通过仍只返回建议，正式执行必须再校验平台证据、权限、库存、执行版本及数量。试算绝不改变订单、库存或外部系统。
5. 无候选返回无命中提示，由人工核对条件，不自动当成审核通过或兜底仓。

## 4. 权限、审计与可观测性

已确认要遵循中台平台、店铺、仓库和敏感数据范围；具体角色矩阵待确认。前端仅提供演示工作台，不宣称已经实现生产授权。

实际提交与核查应保留操作者、请求/链路标识、源状态、目标状态、原因码、有效业务版本、平台/店铺、执行时点和外部证据。原始报文受控存储，普通界面只显示摘要；不保存真实凭据和身份信息。规则变更需记录前后版本、条件、动作、启停和优先级，试算需记录输入版本和结论。批量结果应可关联到每张原业务单，不另建异常单。

## 5. 易仓替代与上线约束

本次只交付可评审原型，不据此认定易仓能力已经替代。

| 现有能力/目标 | 当前差异与待办 | 切换与回退要求 |
| --- | --- | --- |
| 易仓/人工 eBay 标发 → OMS 标发中心 | 实际使用角色、单量、人工操作、部分结果、错误修复与既有 fulfillment 认领规则待确认 | 上线前查询既有记录；按平台+店铺+标发能力单写。影子阶段逐行、逐包裹对账；来源无法认领、重复或数量差异立即停止相应写入。 |
| 易仓审单/分仓/标发规则 → 自研规则 | 原规则条件、优先级、生效时间、叠加策略、日志和例外路径尚待调查 | 先用脱敏样例回放比对；按版本及灰度店铺发布。回退只切换后续规则版本，不改写已提交载荷和履约快照。 |
| 历史数据与在途单 | 迁移范围和保留期、来源归属与对账阈值待确认 | 回退前核查待受理请求；保留订单、标发业务单与全部尝试，禁止因回退在易仓再次盲目提交。 |

生产上线前需指定业务、产品、研发和测试负责人、灰度时间、核对阈值和回退执行人；姓名与截止日期未提供，不推测填入。

## 6. 验收与未验证项

| 用例 | 预期 |
| --- | --- |
| 标发五态切换筛选、查询无结果 | 按标发单统计，空态可以清除条件。 |
| 选择可执行和待核查混合批次 | 二次确认披露可执行/跳过数量；待核查单不提交；结果与勾选一致。 |
| 请求不明后查询确认成功 | 在原单补记成功并增加查询证据，不重复创建标发单。 |
| 已成功/无需标发重复提交 | 被前置状态门禁阻断。 |
| 仅跟踪号、无实际出库或数量超过出库 | 被阻断，不改变已发货订单事实。 |
| eBay 同一行只标发部分应发数量 | 被数量覆盖约束阻断。 |
| 创建、编辑取消、重复名称、空条件 | 有效配置才保存；取消不改变原规则；未完整时提示。 |
| 停用、顺序调整、AND/OR 与多命中试算 | 停用跳过，按当前类型顺序；条件逻辑正确，首命中候选和全部轨迹可见。 |
| 规则命中但订单处于异常、缺付款或未出库 | 候选可显示，但门禁必须明确拦截且不改变订单。 |
| 切换页签后回到标发/规则 | 本次会话单据、尝试、规则和日志保留；刷新重置。 |
| 开启原型标注与桌面三种宽度 | 标签可定位、业务控件仍能点击；不发生页面级横向溢出。 |

未验证：真实平台/仓库接口、权限及角色矩阵、并发幂等、生产持久化、正式规则发布、实时库存与付款、真实业务量性能、易仓双跑和历史认领。相关项须在范围与责任明确后完成接口和集成验收，不把本地模拟通过计作上线验收。
