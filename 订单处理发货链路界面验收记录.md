# 订单处理发货链路界面验收记录

- 日期：2026-09-23。
- 范围：订单处理完整链路界面优化，以及进入订单标发时的关联编号筛选。
- 本地预览：http://127.0.0.1:5173/#/oms/self-fulfillment/processing
- 需求依据：完整项目 `PRD/订单处理功能PRD.md` 第 18 节；关联查询补充于 `PRD/订单标发功能PRD.md` 第 6 节。第 18.5 节所列页签与删减已由用户页面批注确认，其余界面与批量范围仍为评审提案。

## 已完成

1. 按用户页面批注，普通模式使用六态 Tab，仅展示状态和数量；标注模式保留作业任务、对象数量及单位的阶段卡片和说明。两种模式均删除顶部标题整行和“订单规则”快捷入口。
2. 阶段内关注筛选覆盖可审核、审核待处理、可推仓、待核查、明确失败、部分出库及独立标发结果。
3. 待发货突出出库数量、剩余数量、跟踪号、实际发货时间及最近仓库回传；已发货单独展示标发状态、承运商及原因。
4. 快速查看增加四类履约事实摘要、当前下一步和收件信息，按订单进入时列出关联仓库执行。保留审核汇总、日志与唯一全渠道详情。
5. 预检展示执行仓库及计划数量；结果提供进入下一阶段的入口。下单失败指向对应出库单，查看标发按履约单定位。
6. 1280 普通视图的阶段列表可在可用宽度内展示；标注侧栏压缩业务区域时阶段卡片按容器宽度改为紧凑两行，宽表保持内部滚动。
7. PRD、原型标注和 README 同步更新。本轮未发布在线站点。

## 工程验证

| 检查 | 结果 |
| --- | --- |
| `pnpm typecheck` | 通过 |
| `pnpm test` | 11 个测试文件、74 项测试全部通过；包含新增 4 项作业关注与数量边界测试 |
| `pnpm build` | 最终版本通过；保留主包大于 500 kB 的构建提示，无构建错误 |
| `git diff --check` | 通过 |

## 浏览器验证

使用本地虚构样例实际操作，未调用平台、仓库或库存接口。

| 场景 | 观察结果 |
| --- | --- |
| 初始待审核筛选 | 6 张待审订单中 2 张可审核、4 张待处理；选择后切换关注，批量按钮恢复禁用，隐藏对象不保留选择 |
| 审核与预检 | `OMS260912000001` 在详情进入审核预检，核对仓库、数量、版本后通过；产生 `FL-OMS260912000001-R1`，进入待推单 |
| 推仓受理 | 提交后结果为处理中 1、待核查 1；订单仍在待推单，没有生成出库事实 |
| 仓库明确接单 | 对同一履约核查正常演示响应后进入待发货，已出库 0 / 计划 1，剩余 1；跟踪号与出库时间仍显示未回传 |
| 部分出库 | `FL260909000072` 显示已出库 1 / 计划 3、剩余 2，仍属待发货；缺失出库时间不补造 |
| 标发失败 | 已发货筛选得到 2 条标发失败样例，OMS 仍为已发货；查看 `FL-DEMO-CF-04` 标发时精准筛选 `CF-DEMO-260912-004` |
| 仓库明确失败 | “处理失败”携带 `FL-DEMO-260912-7` 进入三方仓出库单，仅显示对应 `OUT-DEMO-260912-7` |
| 导航与页签 | OMS 多列菜单、订单处理菜单、已打开业务页签可切换；全局菜单搜索可检索订单处理 |
| 标注交互 | 可搜索“阶段作业关注”、展开规则和 PRD 引用；标注打开时业务编号查询仍生效 |
| 宽度检查 | 1280×900、1440×900、1920×1080 普通及标注模式实际检查；文档横向宽度分别为 1280、1440、1920，无页面级横向溢出。1280×720 普通模式亦检查可用；表格在内部纵向滚动 |

## 页面批注修订复验

- 普通模式显示六个状态 Tab（状态名与数量），顶部标题整行、订单规则快捷入口和解释性提示已删除或移至标注模式。
- 实际切换待审核/待推单，按系统订单号查询得到对应履约；打开标注后阶段、已应用查询及结果数量保留，显示阶段卡片与说明，关闭后恢复 Tab。
- 普通与标注模式均复查 1280、1440、1920 宽度，无页面级横向溢出；浏览器未记录脚本错误。
- 本次复验 `pnpm typecheck`、`pnpm test`（74 项）、`pnpm build` 全部通过。构建保留既有主包体积提示。
- 勾选跨模式保持由共享选择状态实现；此次浏览器勾选操作未完成，未记为实测通过。

## 边界与待确认

- 原型没有真实 API、数据库持久化、库存占用/释放、权限及实时付款状态校验。刷新浏览器恢复固定样例。
- 本轮正常审核至仓库接单可串联操作；实际出库及标发完整状态通过既有固定样例评审，没有新增人工直接写“已发货”的能力。
- 人工解除审核拦截、部分出库剩余范围、异常恢复、不发货操作、撤单责任及正式批量范围仍需专项评审。
- 本记录证明本地界面与列明交互的验证结果，不证明生产接口或易仓替代已通过验收。
